import { createTheme } from '@mui/material/styles'

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#eb6f25',
      contrastText: 'white',
    },
  },
})

export default customTheme
